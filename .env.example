HOST=127.0.0.1
PORT=4318
PUBLIC_ORIGIN=http://localhost:5173
# Supabase project intended specifically for this pilot. No service-role key needed.
SUPABASE_URL=
SUPABASE_ANON_KEY=
# OpenAI-compatible NVIDIA endpoint, including /v1. Never use VITE_ for secrets.
INFERENCE_BASE_URL=
INFERENCE_MODEL=
INFERENCE_API_KEY=
# Operator-provided health URLs for actual deployed VSS and Riva HTTP gateways.
VSS_HEALTH_URL=
RIVA_HEALTH_URL=
# Dedicated Riva Translate service through the private GPU tunnel
TRANSLATION_BASE_URL=http://127.0.0.1:4320
VSS_EMBED_BASE_URL=http://127.0.0.1:4321
